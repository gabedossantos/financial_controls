# Copy to .env and set your own Neon connection string
DATABASE_URL="postgresql://user:password@host:5432/dbname?sslmode=require"

# Optional: reduce heavy seed volume (default 50000)
SEED_TX_COUNT=2000
